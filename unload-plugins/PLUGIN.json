{
  "commands": [
    {"unload plugins": "unload-plugins-action"}
  ],
  "processes": [
    {"unload-plugins-action": {
      "instructions": "Deactivate plugin mode and acknowledge with '✅ Plugins unloaded. Standard response mode.' Return to normal Claude Code operation without evaluating messages against plugin patterns."
    }}
  ]
}